# Hadiri Implementation Notes

- Root folder `Documents/TAPTU` is now treated as the new project root.
- `apps/web` contains the landing page, login page, PWA setup, and app shell.
- `apps/api` contains the first Express API baseline for auth and dashboard demo data.
- `packages/shared` contains shared TypeScript types between frontend and backend.
- Next production steps: PostgreSQL, Redis, OTP, attendance endpoints, upload flow, and role-specific feature modules.
